{#{# 
====================================================================
TEMPLATE HTML JINJA2 POUR APACHE2
====================================================================
Ce fichier est un template Jinja2 qui sera transformé en page HTML
par Ansible lors du déploiement. Les variables entre {{ "{{" }} }} sont
remplacées par leurs valeurs définies dans le playbook.
==================================================================== 
#}
<!DOCTYPE html>
<html>
<head>
    <title>Hello World</title>
    <style>
        /* Styles CSS pour la page */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            text-align: center;
            padding: 50px;
        }
        h1 {
            color: #0066cc;
            font-size: 3em;
        }
        .container {
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            max-width: 600px;
            margin: 0 auto;
        }
        .host-info {
            margin-top: 20px;
            font-style: italic;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        {# 
        Variable 'hello_message' définie dans le playbook principal (site.yml)
        avec des valeurs différentes selon l'hôte:
        - "Hello World 1" pour host1
        - "Hello World 2" pour host2
        #}
        <h1>{{ hello_message }}</h1>
        <div class="host-info">
            {# 
            Variable 'ansible_hostname' est une variable automatique d'Ansible
            qui contient le nom d'hôte du serveur cible
            #}
            Serveur: {{ ansible_hostname }}
        </div>
    </div>
</body>
</html>
